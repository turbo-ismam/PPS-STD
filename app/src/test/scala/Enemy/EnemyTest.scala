package Enemy

import org.junit.runner.RunWith
import org.scalatest.funsuite.AnyFunSuite
import org.scalatestplus.junit.JUnitRunner

class EnemyTest {

  @RunWith(classOf[JUnitRunner])
  class AppSuite extends AnyFunSuite {

    test("Enemy test example") {
      assert(true)
    }
  }
}
